<ui:FluentWindow x:Class="VSL.UI.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="VintageStoryServerLauncher (VSL)"
                 Width="1200"
                 Height="780"
                 MinWidth="1000"
                 MinHeight="640"
                 Background="{DynamicResource VslAppBackgroundBrush}"
                 Icon="{DynamicResource VslAppLogoImage}"
                 ExtendsContentIntoTitleBar="True"
                 WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Margin="12,0,0,6"
                     Background="{DynamicResource VslAppBackgroundBrush}"
                     ShowMinimize="True"
                     ShowMaximize="True"
                     ShowClose="True"
                     Title="Vintage Story Server Launcher">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="{DynamicResource VslAppLogoImage}" />
            </ui:TitleBar.Icon>
            <ui:TitleBar.Header>
                <Border Height="28" VerticalAlignment="Center" Background="Transparent">
                    <TextBlock Text="VSL" FontWeight="SemiBold" Foreground="#5D4E37" VerticalAlignment="Center" FontSize="13" />
                </Border>
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="0,0,12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="6" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{DynamicResource VslPanelBackgroundBrush}"
                    BorderBrush="{DynamicResource VslPanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="6,6">
                <StackPanel>
                    <Button Content="版本与档案" Tag="VersionProfilesPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                    <Button Content="服务器配置" Tag="ServerSettingsPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                    <Button Content="高级 JSON" Tag="AdvancedJsonPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                    <Button Content="存档管理" Tag="SavesPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                    <Button Content="地图预览" Tag="MapPreviewPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                    <Button Content="Mod 管理" Tag="ModsPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                    <Button Content="机器人配置" Tag="Vs2QQConfigPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,12" />
                    
                    <Separator Margin="0,4,0,12" Background="{DynamicResource VslPanelBorderBrush}" />
                    
                    <Button Content="控制台" Tag="ConsolePage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                    <Button Content="机器人" Tag="Vs2QQRunnerPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                    <Button Content="设置" Tag="AppSettingsPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                    <Button Content="关于" Tag="AboutPage" Click="NavigationButton_Click" Style="{StaticResource VslToolbarButtonStyle}" Margin="0,0,0,4" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Background="{DynamicResource VslPanelBackgroundBrush}"
                    BorderBrush="{DynamicResource VslPanelBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Margin="0,0,0,8"
                    Padding="0">
                <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden" />
            </Border>
        </Grid>

        <ui:SnackbarPresenter x:Name="RootSnackbarPresenter"
                              Grid.Row="1"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Bottom"
                              Margin="0,0,24,18" />
    </Grid>
</ui:FluentWindow>
